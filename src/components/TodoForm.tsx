import * as React from "react";

const TodoForm: React.FC = (props) => {
  return (
    <>
      <form id="todo-form">
        <input type="text" />
      </form>
    </>
  );
};

export default TodoForm;
